"use client";import Providers from "../providers";import {Page,Loading} from "@/components/Page";import {useData} from "@/components/DataProvider";
function View(){const {data}=useData();if(!data)return <Loading/>;const cats=[...new Set(data.rules.map(r=>r.category))];return <Page title="Rules" subtitle="The official OKFL rule book."><div className="stack">{cats.map(c=><article className="card" key={c}><h2>{c}</h2>{data.rules.filter(r=>r.category===c).map(r=><div className="rule" key={r.id}><b>{r.id}</b><p>{r.rule}</p></div>)}</article>)}</div></Page>};export default function Rules(){return <Providers><View/></Providers>}