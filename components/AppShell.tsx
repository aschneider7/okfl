"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {useState} from "react";
const links=[["/","Home"],["/franchises","Franchises"],["/compare","Compare"],["/trades","Trades"],["/drafts","Drafts"],["/keepers","Keepers"],["/records","Records"],["/time-machine","Time Machine"],["/rules","Rules"]];
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);return <div className="shell"><aside className={open?"sidebar open":"sidebar"}><div className="brand"><small>Obama Keeper Fantasy League</small><b>OKFL OS</b><span>v0.6 Rebuild</span></div><nav>{links.map(([href,label])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={path===href?"active":""}>{label}</Link>)}</nav></aside><header className="mobileHeader"><button onClick={()=>setOpen(!open)}>☰</button><b>OKFL OS</b><span>v0.6</span></header><main className="main">{children}</main>{open&&<button className="backdrop" onClick={()=>setOpen(false)} aria-label="Close menu"/>}</div>}