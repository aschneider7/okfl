"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function CommissionerLogin(){const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const response=await fetch("/api/commissioner/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error||"Login failed");return;}router.refresh();}return <div className="commissionerGate"><form onSubmit={submit} className="commissionerLogin card"><span className="eyebrow">Private area</span><h1>Commissioner Login</h1><p>Enter the commissioner password to manage Sleeper synchronization and data integrity.</p><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="Commissioner password"/><button disabled={loading}>{loading?"Checking…":"Unlock Commissioner OS"}</button>{error&&<div className="loginError">{error}</div>}</form></div>}
